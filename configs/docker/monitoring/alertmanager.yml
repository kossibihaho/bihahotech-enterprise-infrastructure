global:
  resolve_timeout: 5m

route:
  receiver: "default"
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: "default"
    # Aucune notification externe configurée pour l'instant.
    # Exemple pour ajouter un webhook plus tard :
    # webhook_configs:
    #   - url: "http://exemple.local/webhook"

